{
  "private": true,
  "name": "@kiwi/tooling",
  "description": "🥝",
  "author": "Christian Kaisermann <christian@kaisermann.me>",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "publish": "lerna publish --force-publish --conventional-commits",
    "lint": "eslint --ext .js packages/",
    "fix": "eslint --ext .js packages/ --fix; prettier --write packages/**/*.js"
  },
  "devDependencies": {
    "@types/eslint": "^7.2.13",
    "eslint": "^7.13.0",
    "lerna": "^3.22.1",
    "prettier": "^2.2.0"
  }
}
